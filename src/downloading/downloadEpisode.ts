//import remote from "remote";
import { Item } from "rss-parser";
const fs = require("fs");
console.log("CACHE PATH", fs);

export const downloadEpisode = (episode: Item) => {
  //const cachePath = app.getPath("appData");
};
